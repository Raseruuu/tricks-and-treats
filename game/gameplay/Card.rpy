init python:
    class Card:
        def __init__(self,text,effect,grafic):
            self.text = text
            self.effect = effect
            self.grafic = grafic
        
        def playCard():
            effect.playEffect()
            